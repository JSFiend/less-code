import EventEmitter from 'eventemitter3';

const EE = new EventEmitter();


const eventCenter = {};

sequentialEmitter.on('addEvent', (eventName, listener) => {
  eventListeners.push({ eventName, listener });
});


export function addEvent(id, eventName, ) {
  eventCenter[id] = eventCenter[id] || [];

  eventCenter[id].push()
}