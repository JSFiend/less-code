

import { ComponentInstance } from "~types/index";

// 注册事件
function registeredEvents(instanceList: ComponentInstance[]) {

}


// 触发事件


function emitEvent(uniqueId: string, event: string, params?: Record<string, any>) {
  
}